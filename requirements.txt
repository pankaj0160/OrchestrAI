# =============================================================================
# OrchestrAI — Multi-agent pipeline (Search → Reader → Writer → Critic)
# Stack: Groq + Mistral | Tavily search | Beautiful Soup parsing | LCEL + LangGraph
# Python 3.12+
# =============================================================================

# --- Environment & config ---
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- LangChain core (LCEL, Runnables, prompts, tools) ---
langchain>=0.3.14
langchain-core>=0.3.28
langchain-text-splitters>=0.3.4
langchain-community>=0.3.14

# --- LLM providers (Groq + Mistral) ---
langchain-groq>=0.2.4
groq>=0.13.0
langchain-mistralai>=0.2.10
mistralai>=1.5.0

# --- ReAct agents & multi-agent orchestration ---
langgraph>=0.2.60
langgraph-checkpoint>=2.0.0

# --- Tavily live web search (Search agent) ---
langchain-tavily>=0.1.5
tavily-python>=0.5.0

# --- HTTP + HTML extraction (Reader agent — Beautiful Soup, no API key) ---
requests>=2.32.3
httpx>=0.28.0
beautifulsoup4>=4.12.3
lxml>=5.3.0
html5lib>=1.1

# --- Dev / CLI ---
rich>=13.9.0